{{#ui-block/form as |form|}}
  {{#form.row as |row|}}
    {{row.button title='Project' action=(action 'project')}}
    {{row.button title='Group' action=(action 'group')}}
  {{/form.row}}
{{/ui-block/form}}
